version: "3.9"

services:
  backend:
    build: ./back
    container_name: fastapi_server
    ports:
      - "8000:8000"

  frontend:
    build: ./front
    container_name: streamlit_app

    ports:
      - "80:8501"

    depends_on:
      - backend